/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/compound',
  assetPrefix: '/compound/'
}

module.exports = nextConfig
